#!/bin/bash

PREFIX_DIR="@CMAKE_INSTALL_PREFIX@"
@PROJECT_NAME_UPPER_CASE@_DIR="${PREFIX_DIR}/@@BUILD_PROJECT_NAME_UPPER_CASE@_INSTALL_DIR@"
EXECUTABLE="@PROJECT_NAME@"

cd "${@BUILD_PROJECT_NAME_UPPER_CASE@_DIR}" && ./${EXECUTABLE} "$@"
